use strict;

#
# Place your configuration directives here.  They will override those in
# earlier files.
#
# See /usr/share/doc/amavisd-new/ for documentation and examples of
# the directives you can use in this file
#

# Number of processes that amavisd-new uses. Make sure that
# /etc/postfix/master.cf also reflects this value.
$max_servers = 20;

# Do not add spam subject tag.
$sa_spam_subject_tag = undef;
# Do not add unchecked subject tag.
$undecipherable_subject_tag = undef;

# Always add spam info headers.
$sa_tag_level_deflt = undef;

# Always pass on spam. Let the user decide what to do with it.
$final_spam_destiny = D_PASS;

# Don't quarantine viruses, spam is passed on always with modified headers.
$QUARANTINEDIR = undef;

@local_domains_acl = ( ".hcs.harvard.edu"<%= @secondary_domains.map{ |domain| ", \"." + domain + "\"" } %> );

#------------ Do not modify anything below this line -------------
1;  # ensure a defined return
