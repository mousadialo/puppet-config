---
login:
  parameters:
    machine_type: login
  environment: production
web:
  parameters:
    machine_type: web
  environment: production
generic:
  parameters:
    machine_type: generic
  environment: production
mail:
  parameters:
    machine_type: mail
  environment: production
lists:
  parameters:
    machine_type: lists
  environment: production
file:
  parameters:
    machine_type: file
  environment: production
